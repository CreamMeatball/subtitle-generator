# M1 코어 파이프라인 의존성
faster-whisper>=1.1.0
# ffmpeg 는 시스템/동봉 바이너리 사용 (pip 패키지 아님)
#
# GPU(CUDA) 사용 시: NVIDIA 드라이버 + CUDA 12 / cuDNN 9 런타임 필요.
#   배포 빌드에서는 해당 런타임을 동봉하거나 최초 실행 시 다운로드한다.
# CPU 전용은 추가 런타임 없이 동작한다.
